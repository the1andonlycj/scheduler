import React from "react";
import Show from "./Show";
//import ReactDOM from "react-dom";

import "./styles.scss";

export default function Appointment() {
  return (
    <article className="appointment">
      {/* < Show onClick={props.onClick}/>
     */}
    
    </article>

  )
}